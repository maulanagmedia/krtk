<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/nav_cs"
        android:title="Chat"
        android:orderInCategory="101"
        app:showAsAction="always"
        android:icon="@drawable/ic_cs"/>

    <item
        android:id="@+id/empty"
        android:title="Option"
        android:orderInCategory="101"
        app:showAsAction="always"
        android:icon="@drawable/ic_option">

        <menu>

            <item android:title="Profile"
                android:id="@+id/option_profile"
                app:showAsAction="ifRoom"
                android:icon="@mipmap/ic_profile"/>

            <item android:title="Ubah Password"
                android:id="@+id/option_ubah_password"
                app:showAsAction="ifRoom"
                android:icon="@mipmap/ic_password1"/>

            <item android:title="Hentikan Akses Aplikasi"
                android:id="@+id/option_kill_access"
                app:showAsAction="ifRoom"
                android:visible="false"
                android:icon="@mipmap/ic_kill_app"/>

            <item android:title="Logout"
                android:id="@+id/option_logout"
                app:showAsAction="ifRoom"
                android:icon="@mipmap/ic_close"/>
        </menu>
    </item>
</menu>